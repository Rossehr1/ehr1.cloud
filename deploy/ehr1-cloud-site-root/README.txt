Optional static pages for ehr1.cloud (document root)
====================================================

- `index.html` — minimal home
- `certified-backup.html` — placeholder for a "certified backup" page; replace with your real copy or restore from backup

Upload all `*.html` here with:
  cd deploy\ehr1-cloud-site-root
  .\deploy-site-root.ps1

Do not overwrite a CMS you still need. Restore lost pages from Hostinger backups if available.
