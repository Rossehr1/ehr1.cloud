# Paste this block inside the existing `server { ... }` for ehr1.cloud — before `location /` if you can.
# Nginx does not read `.htaccess`; this mirrors `deploy/ehr1-cloud-site-root/.htaccess` (`RewriteRule ^ehr1-data$`).
# After editing: `nginx -t && systemctl reload nginx`

    location = /ehr1-data {
        return 301 /ehr1-data/;
    }
